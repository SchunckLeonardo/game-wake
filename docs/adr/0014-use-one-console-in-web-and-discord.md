# Use one Console in Web and Discord

GameWake provides one responsive GameWake Console that runs in a normal browser and inside Discord as an Activity. Slash commands remain fast shortcuts and deep-link complex tasks into the appropriate Console screen; every surface uses the same API, authorization, and Game Template schemas. This avoids parallel product implementations while preserving Discord-first convenience and a complete standalone Web UI.
